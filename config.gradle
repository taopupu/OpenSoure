ext {
    // Sdk and tools
    compileSdkVersion = 27
    buildToolsVersion = "27.0.0"
    minSdkVersion = 19
    targetSdkVersion = 27
    versionCode = 1010200
    versionName = "1.1.2"

    // App dependencies
    supportLibraryVersion = "27.1.0"
}